# AI Work Log - 2026-08-02

## 2026-08-02T09:54:14+10:00

### Prompt

> add a recursive flag which will scan for markdown files in given/current directory if a file path is not provided

### Model

Claude Opus 4.6 via Antigravity

### Actions

- Modified `src/cli.rs` — added `-r`/`--recursive` flag to `Args` struct, added `detect_input_source_recursive` and `expand_recursive_patterns` functions that convert directory arguments to `dir/**/*.md` glob patterns, updated `finalize_cli` to route through recursive detection when flag is set
- Modified `src/error.rs` — added `NoMarkdownFilesFound` error variant for when recursive scan finds zero `.md` files, added error message test and included in exhaustive exit-code test
- Added 7 unit tests in `src/cli.rs`: flag parsing (short/long/default), directory scanning, nested subdirectory scanning, mixed dir+file arguments, empty directory error, and finalize_cli integration
- Decision: reused existing `glob` crate's `**` pattern instead of adding `walkdir` dependency — keeps dependency count minimal

### Outcome

✅ Added `--recursive`/`-r` flag that scans directories for `**/*.md` files; all 187 tests pass (128 unit + 10 lib + 49 integration)

## 2026-08-02T15:15:23+10:00

### Prompt

> Add a .pre-commit-hooks.yaml as well as git-commit.nix module for distributing smt as a pre-commit hook

### Model

Claude Opus 4.6 (Thinking) via Antigravity

### Actions

- Created `.pre-commit-hooks.yaml` — distribution file for pre-commit framework users; provides two hooks: `sort-markdown-tables` (auto-fix with `-i`) and `sort-markdown-tables-check` (check-only with `--check`), both targeting `*.md` files using `language: rust`
- Created `nix/git-commit-module.nix` — Nix module for `cachix/git-hooks.nix` users; follows the same pattern as `nix/treefmt-module.nix` with `enable` and `package` options, runs `smt -i` on markdown files as a system hook
- Modified `flake.nix` — added `gitHooksModules` output alongside existing `treefmtModules`, exposing the git-commit module for Nix consumers
- Modified `README.md` — replaced local pre-commit example with full Integrations section covering pre-commit framework, Nix git-hooks.nix module, and Nix treefmt module
- Decision: provided two pre-commit hook variants (fix vs check-only) to match different CI/local workflows

### Outcome

✅ Distribution hooks created; flake evaluates cleanly with `nix flake check --no-build`

## 2026-08-02T21:43:23+10:00

### Prompt

> Split the README into docs

### Model

Claude Sonnet 4.6 (Thinking) via Antigravity

### Actions

- Created `docs/installation.md` — installation section (cargo, binaries, Nix Flakes)
- Created `docs/usage.md` — CLI usage, table format, sort config attributes, exit codes
- Created `docs/integrations.md` — pre-commit, git-hooks.nix, treefmt, CI/CD, bulk formatting
- Created `docs/architecture.md` — pipeline, algorithm, module layout, dependency table
- Modified `README.md` — replaced full content with landing page (intro, features, quick start, links to docs)

### Outcome

✅ README split into 4 docs pages; all files formatted cleanly
